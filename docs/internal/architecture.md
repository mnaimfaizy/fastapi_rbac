# System Architecture (moved)

The canonical architecture narrative now lives in:

**[docs/reference/architecture.md](../reference/architecture.md)**

That page is the single source of truth for system architecture, directory layout, domain model overview, and the Redis allowlist auth flow. AI harness files should link there instead of embedding a second copy.
