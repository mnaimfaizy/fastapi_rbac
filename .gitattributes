# Autodetect text files
* text=auto

# ...Unless the name matches the following
# overriding patterns

# Definitively text files
*.txt text
*.c text
*.h text
*.py text

# Ensure those won't be messed up with
*.jpg binary
*.data binary

# Ensure that shell scripts have LF line endings
*.sh text eol=lf
